
## Steps

1. Review or modify the sample configuration file:
   `sample_config.yaml`

2. Run the main automation script from the project root directory:

   ```bash
   python scripts/run_flow.py --config examples/sample_config.yaml